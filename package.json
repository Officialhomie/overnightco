{
  "name": "overnightco",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "workspaces": ["web"],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm run dev -w overnightco-web",
    "build": "npm run build -w overnightco-web",
    "demo": "cd web && npx tsx scripts/trigger-demo.ts"
  }
}
